//!!! required dinput8.lib dxguid.lib in link options/object modules


const int lbutton=12,rbutton=13,mbutton=14
      ,mousex=0,mousey=4,moverflow=-1;

bool initdinput();
    // ИНИЦИАЛИЗИРУЕТ directinput
    // обязательно вводится перед инициализацией мыши или клавиатуры

bool initmouse(void *mwnd,bool mlevel,int mbuffersize,bool axismode);
    // Инициализация мыши
    // mvnd - указатель (Header) на окно программы., например Form1->Header
    // mlevel -уровень доступа к мыши: false -нормальный (рекомендуется),true -монопольный
    // mbuffersize -размер буфера сообщений мыши
    // axis -сообщения мыши при изменении положения:
    // false - смещение относительно предыдущей позиции
    // true - абсолютные значения

void getmouse (int &mx,int &my,bool &lb,bool &rb,bool &mb);
  // возвращает состояние мыши.
  // mx-горизонтальная координата
  // my-вертикальная координата
  // lb - левая кнопка нажата ?
  // rb - правая кнопка нажата ?
  // mb- средняя кнопка нажата ?

bool getbufmouse(int &minfo,int &mdata);
  // возвращает состояние мыши.Возвращает сообщения мыши.
  // Использует буффер.
  // minfo- сообщение мыщи:
  //        mousex-мышь сместилась горизонтально
  //        mousey-мышь сместилась вертикально
  //        lbutton -была нажата левая клавиша мыши.
  //        rbutton -была нажата правая клавиша мыши.
  //        mbutton -была нажата средняя клавиша мыши.

void closemouse();
  // удаляет обьект мышь и освобождает ресурсы
  // рекомендуется вызвать , когда мышь уже не нужна
  // или при закрытии программы

 bool initkeyboard(void *mwnd,bool klevel,int kbuffersize);
 // Инициализация мыши
    // kvnd - указатель (Header) на окно программы., например Form1->Header
    // kevel -уровень доступа к клавиатуре: false -нормальный (рекомендуется),true -монопольный
    // kbuffersize -размер буфера сообщений клавиатуры

 bool getkeys (unsigned char ka[256]);
   // возвращает состояние клавиатуры.
   // ка -массив состояния клавиш: 0 - не нажата ,128 - нажата

bool presskey(unsigned char mkey);
   // Возвращает true если клавиша mkey нажата иначе false

 bool getbufkeys(int &kinfo,bool &kdata);
   // Возвращает сообщения клавиатуры.
   // Использует буффер.
   // Если результат false то произошло переполнение буффера или не инициализирована клавиатура
   // kinfo- номер нажатой клавиши
   // kdata - если true -нажата,false -отпущена
 int getkey();//!!!возвращает, если клавиша была нажата(если ничего не изм. -возв 0)
 
 void resetkeyboard();
	 //очищает буфер клавиатуры

 void closekeyboard();
  // удаляет обьект клавиатура и освобождает ресурсы
  // рекомендуется вызвать , когда клавиатура уже не нужна
  // или при закрытии программы

